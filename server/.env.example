
# ===========================================
# DATABASE CONFIGURATION
# ===========================================
# For local development (individual variables)
DB_USER=postgres
DB_HOST=localhost
DB_NAME=business_accounting
DB_PASSWORD=your_password_here
DB_PORT=5432

# For Railway/Production (Railway provides this automatically)
# DATABASE_URL=postgresql://username:password@host:port/database

# ===========================================
# APPLICATION CONFIGURATION
# ===========================================
NODE_ENV=development
PORT=3000

# ===========================================
# SECURITY
# ===========================================
JWT_SECRET=your_jwt_secret_here_minimum_32_characters

# ===========================================
# PRODUCTION SETTINGS (for Railway deployment)
# ===========================================
# Replace with your actual Railway app URL
FRONTEND_URL=https://your-app-name.railway.app
ALLOWED_ORIGINS=https://your-app-name.railway.app

# ===========================================
# RAILWAY DEPLOYMENT NOTES
# ===========================================
# 1. Railway automatically provides DATABASE_URL
# 2. Set NODE_ENV=production in Railway dashboard
# 3. Generate a strong JWT_SECRET (32+ characters)
# 4. Update FRONTEND_URL with your Railway app URL
# 5. ALLOWED_ORIGINS should match your Railway domain